# Analog clock 
A simple and visually appealing Analog Clock created using HTML, CSS, and JavaScript. This project demonstrates working with DOM manipulation, CSS transformations, and real-time JavaScript functions to display the current system time dynamically.

📌 Features

Displays real-time current time (hours, minutes, seconds)

Smooth rotation of clock hands

Responsive and minimal design

Built using vanilla HTML, CSS & JavaScript (no external libraries)

🛠️ Technologies Used
Technology	Purpose
HTML	Structure of the clock
CSS	Styling & responsive design
JavaScript	Time calculation & updating hands dynamically
🔧 Installation & Setup

Clone the repository:

git clone https://github.com/SuruchiK26/Analog-clock.git


Navigate to the project folder:

cd Analog-clock


Open index.html in your browser:

Double click the file, or

Use Live Server (if using VS Code)

📂 Project Structure
Analog-clock/
│── index.html      # Main HTML file
│── style.css       # Styling for clock layout
│── script.js       # JavaScript for time logic
└── README.md       # Project documentation
